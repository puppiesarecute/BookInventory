@model IEnumerable<BookInventory.Models.Book>

@{
    ViewBag.Title = "Index";
}

<legend>Library</legend>
<div class="container-fluid row">
    @foreach (var item in Model)
    {
        <a href=""><img src="@item.CoverThumbnailPath" alt="Book" class="img-thumbnail"/></a>
    }

</div>

